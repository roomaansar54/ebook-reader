import { MatchCountPipe } from './match-count.pipe';

describe('MatchCountPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchCountPipe();
    expect(pipe).toBeTruthy();
  });
});
